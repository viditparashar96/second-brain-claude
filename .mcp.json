{
  "mcpServers": {
    "second-brain": {
      "command": "${CLAUDE_PLUGIN_ROOT}/scripts/start_mcp.sh",
      "args": [],
      "env": {
        "SECOND_BRAIN_HOME": "${HOME}/.second-brain",
        "CLAUDE_PLUGIN_ROOT": "${CLAUDE_PLUGIN_ROOT}"
      }
    }
  }
}
