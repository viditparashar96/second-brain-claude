{
  "description": "Second Brain lifecycle hooks — memory injection, context persistence, security guardrails",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "if command -v python3 >/dev/null 2>&1; then python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/session_start.py\"; else python \"${CLAUDE_PLUGIN_ROOT}/scripts/session_start.py\"; fi",
            "timeout": 10000,
            "statusMessage": "Loading Second Brain memory..."
          }
        ]
      }
    ],
    "PreCompact": [
      {
        "matcher": "auto",
        "hooks": [
          {
            "type": "command",
            "command": "if command -v python3 >/dev/null 2>&1; then python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/pre_compact.py\"; else python \"${CLAUDE_PLUGIN_ROOT}/scripts/pre_compact.py\"; fi",
            "timeout": 15000,
            "statusMessage": "Saving context before compaction..."
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "if command -v python3 >/dev/null 2>&1; then python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/session_end.py\"; else python \"${CLAUDE_PLUGIN_ROOT}/scripts/session_end.py\"; fi",
            "timeout": 15000,
            "statusMessage": "Saving session context..."
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash|Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "if command -v python3 >/dev/null 2>&1; then python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/guardrails.py\"; else python \"${CLAUDE_PLUGIN_ROOT}/scripts/guardrails.py\"; fi",
            "timeout": 5000,
            "statusMessage": "Checking security guardrails..."
          }
        ]
      }
    ],
    "Stop": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "if command -v python3 >/dev/null 2>&1; then python3 \"${CLAUDE_PLUGIN_ROOT}/scripts/stop_autolog.py\"; else python \"${CLAUDE_PLUGIN_ROOT}/scripts/stop_autolog.py\"; fi",
            "timeout": 5000
          }
        ]
      }
    ]
  }
}
